// This file is not used in production
// Emergent's infrastructure handles routing:
// - /api/* -> backend (port 8001)
// - /* -> frontend (port 3000)

console.log("API proxy placeholder - not used in Emergent deployment");
process.exit(0);
